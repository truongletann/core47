# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Pinned to SDK 54 deliberately (2026-08-11) — the App Store's current Expo Go
release only supports SDK 54; a SDK 57 scaffold couldn't be opened in
Expo Go at all ("Project is incompatible with this version of Expo Go").
Don't bump past whatever SDK the installed Expo Go client actually
supports without checking first (App Store listing / device test), or
Expo Go breaks again.
